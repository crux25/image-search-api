{
  "name": "image-search-api",
  "version": "1.0.0",
  "description": "Allows individuals to Search for images and browse recent search queries. ",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Amos Ezeme",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.2",
    "mongoose": "^4.10.2"
  }
}
